@model List<AppoinmentViewModel>
@{
        ViewData["Title"] = "Home Page";
}

               <table class="table">
                   <thead>
                       <tr>
                           <th scope="col">#</th>
                           <th scope="col">Date</th>
                           <th scope="col">Capacity</th>
                           <th scope="col">Confirmation</th>
                           <th scope="col">Action</th>

                       </tr>
                   </thead>
                   <tbody>
                       @{int SlNo = 0;}
                       @foreach (var item in Model)
                       {
                       <tr>
                           <th scope="row">@(SlNo += 1)</th>
                           <td>@Html.DisplayFor(x => item.Date)</td>
                           <td>@Html.DisplayFor(x => item.Capacity)</td>
                           <td>@Html.DisplayFor(x => item.IsConfirmed)</td>
                           <td><a asp-action="View" asp-route-id="@item.Id" class="btn btn-outline">View</a></td>
                       </tr>
                       }

                   </tbody>
               </table>